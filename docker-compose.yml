services:
  mysql:
    image: debezium/example-mysql:latest
    container_name: mysql
    hostname: mysql-keycloak
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: keycloak
      MYSQL_USER: keycloak
      MYSQL_PASSWORD: keycloak
      MYSQL_ROOT_HOST: '%'
      MYSQL_AUTHENTICATION_PLUGIN: mysql_native_password
    ports:
      - 3306:3306
    volumes:
      - mysql-data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - keycloak-network
  keycloak:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: keycloak
    environment:
#      - KEYCLOAK_USER=admin
#      - KEYCLOAK_PASSWORD=admin
#      - DB_VENDOR=MYSQL
#      - DB_ADDR=mysql-keycloak
#      - DB_DATABASE=keycloak
#      - DB_PORT=3306
#      - DB_USER=keycloak
#      - DB_PASSWORD=keycloak
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin

    ports:
      - 8080:8080
    command:
      - start-dev
    volumes:
      - keycloak-data:/opt/jboss/keycloak/standalone/data
      - D:\\outside-project\\keycloak-home-idp-discovery.jar:/opt/keycloak/providers/keycloak-home-idp-discovery.jar
    networks:
      - keycloak-network
    depends_on:
      - mysql

volumes:
  keycloak-data:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
#      device: '/opt/keycloak/data'
      device: 'D:\\docker-data\\keycloak'
#  provider-data:
#    driver: local
#    driver_opts:
#      type: 'none'
#      o: 'bind'
#      device: 'C:\\Users\\HLC\\Documents\\docker-mount\\image-data\\keycloak-provider\\keycloak-home-idp-discovery.jar'
  mysql-data:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
#      device: '/var/lib/mysql'
      device: 'D:\\docker-data\\mysql'

networks:
  keycloak-network:
    driver: bridge